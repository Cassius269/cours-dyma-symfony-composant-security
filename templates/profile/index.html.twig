{% extends 'base.html.twig' %}

{% block title %}Hello ProfileController!
{% endblock %}

{% block body %}
	{% if user %}
		<h1>Le profil de
			{{app.user.name }}</h1>

		<pre>
                {{ dump(app.user) }}
            </pre>
	{% endif %}
{% endblock %}
